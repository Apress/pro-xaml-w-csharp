<Page
    x:Class="App3.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App3"    
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Margin" Value="10" />
        </Style>
    </Page.Resources>    
        <Grid x:Name="mainGrid">
            <!--<Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>-->
            <!--<TextBlock x:Name="pixelResolution" />
            <TextBlock x:Name="displayResolution"
                       Grid.Row="1" />
            <StackPanel Orientation="Horizontal"
                        Grid.Row="2">
                <TextBlock Text="Grid Actual Width: " />
                <TextBlock x:Name="gridWidthDetails"
                           Text="{Binding Path=ActualWidth, ElementName=mainGrid}" />
            </StackPanel>-->
            <Image Source="MyImages/star.png" />
    </Grid>
</Page>

